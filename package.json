{
  "name": "iatf-monorepo",
  "version": "0.0.1",
  "private": true,
  "packageManager": "npm@11.12.1",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "dev:landing": "turbo dev --filter=@iatf/landing",
    "dev:tool": "turbo dev --filter=@iatf/tool",
    "build:landing": "turbo build --filter=@iatf/landing",
    "build:tool": "turbo build --filter=@iatf/tool"
  },
  "devDependencies": {
    "turbo": "^2.5.3"
  }
}
